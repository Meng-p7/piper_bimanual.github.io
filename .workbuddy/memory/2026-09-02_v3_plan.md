# Piper 双臂毛巾项目主页 — v3 Plan (2026-09-02 晚)

## 0. v2 → v3 关键纠正（用户已明确指出，必须照做）

| # | 之前错在哪 | 这次怎么改 |
|---|---|---|
| 1 | 写"五相机同步录制" | 改回**三相机**：左腕 + 右腕 + 顶部 Orbbec；五相机方案是历史弯路，简短一句提一下即可 |
| 2 | 顶栏 12 个链接 | 缩到 **5 个**：Overview · System · Learning · Results · Status |
| 3 | 颜色写"三色 + 一个未见的探测" | 改为**四种颜色全部展示**：红 / 蓝 / 绿 / 黄，每张配实拍图，并标注各自在数据集中的角色 |
| 4 | 预训练只写"100 条示教" | 改为**蓝毛巾 + 只做抓角抬升 + chunk_size=150（横扫后的最优值）** |
| 5 | HIL 写"红、绿毛巾" | 写**红 + 绿，101 episodes**（与 v2 一致，但要明确这是 fine-tuning 数据） |
| 6 | §11 写"明天的 C5 Gate A 36 次 / Gate B 45 次" | **整段移除**。网站是"展示成果与项目"的地方，不写未来实验计划 |

## 1. 新结构（5 节，对应 5 个 nav 链接）

| 章节 | slug | 内容 |
|---|---|---|
| **§1 Overview** | `#overview` | Hero：真视频（叠毛巾.mp4）+ 一句话主线 + 三个 status pill + 一次跳转 |
| **§2 System** | `#system` | 4-Piper 架构图 + 遥操（teleop.mp4）+ 自研细长夹爪 + **三相机**说明 |
| **§3 Learning** | `#learning` | C0→C4 时间线 + 抓角稳定 + HIL 流程（红+绿）+ action chunk=150 调参结果 |
| **§4 Results** | `#results` | 4 张毛巾实拍 + 角色标签 + "已展示 / 定性 / 进行中" 三档卡 |
| **§5 Status** | `#status` | Done / In Progress / Future 三栏 + 4 条 Engineering Notes（不进 nav） |

> Engineering Notes 是"节奏活点"，作为 §5 末尾的 lab-note 网格，不占用顶级 nav。

## 2. 关键事实表（不许改，照搬）

| 字段 | 值 |
|---|---|
| 机器人 | 4 × Piper（2 Master + 2 Slave） |
| 控制栈 | ROS 2 Control（Pixi workspace，dream25851/physical_ai_runtime） |
| 计算 | Intel NUC（机器人侧，实时）+ RTX 4090（学习侧，训练/推理） |
| 输入 | 左腕 RGB + 右腕 RGB + 顶部 Orbbec + 14-D 机器人状态 |
| 预训练数据 | 100 episodes · **蓝毛巾** · 只做抓角抬升 |
| 预训练模型 | ACT · ResNet-18 · `chunk_size=150` · 20k steps（横扫后最优） |
| HIL 数据 | 101 episodes · **红 + 绿** · 含偏位、纠偏、完整折叠 |
| 微调模型 | 在 base20k 上继续训 10k HIL steps |
| 定性结果 | 红、绿毛巾上完整抓起到折好 |
| 4 种毛巾 | 红 / 蓝 / 绿 / 黄（黄尚未用于训练集，仅作为 lab 色卡展示） |

## 3. 视觉风格（参照 Human Aakib 截图）

- 保留 indigo/violet 主色，但更克制；
- Hero 用真实视频（叠毛巾.mp4）做右侧媒体位，左侧文字 + status pill + 双 CTA；
- 顶部 nav 收成 5 项；
- 减少"lab notebook"装饰，工程笔记作为§5 末尾的紧凑卡；
- 多处用实拍素材代替占位图：
  - Hero 视频 → `assets/videos/hero_demo.mp4`
  - Teleop 视频 → `assets/videos/teleoperation.mp4`
  - 4 张毛巾 → `assets/images/towel_{red,blue,green,yellow}.jpg`

## 4. 实施顺序

1. ✅ v3 plan 写完（本文）；
2. → 媒体已落位（hero_demo.mp4 / teleoperation.mp4 / 4 张毛巾 jpg）；
3. → 重写 `index.html`（英文 5 节版）；
4. → 重写 `zh-cn/index.html`（中文 5 节版，与英文对齐）；
5. → 调整 `css/style.css`（裁剪 12 节样式，新增 5 节版所需的 hero / colors / 视频卡样式）；
6. → 微调 `js/main.js`（无大改，保留 reveal / mobile nav / ripple）；
7. → 本地预览 + 截图核验。
